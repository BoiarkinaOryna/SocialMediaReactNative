import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    chatContainer: {
        padding: 10,
        gap: 10
    },
    sameSenderMessages: {
        padding: 5,
        gap: 10
    },
    myMessages: {
        alignItems: "flex-end"
    }
})