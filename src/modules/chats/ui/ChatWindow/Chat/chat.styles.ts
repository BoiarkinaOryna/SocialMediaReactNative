import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    chatContainer: {
        padding: 10,
        gap: 10,
        // height: 500,
        // flex: 1
    },
    sameSenderMessages: {
        alignItems: "flex-start",
        padding: 5,
        gap: 10
    },
    myMessages: {
        alignItems: "flex-end"
    }
})