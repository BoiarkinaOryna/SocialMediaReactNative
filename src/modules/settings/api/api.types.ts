export type EditableUserData = {
    token?: string,
    name?: string,
    surname?: string,
    pseudonym?: string,
    username?: string,
    birthDate?: string,
    email?: string
}
export type AlbumData = {
    name: string,
    theme: string,
    year: Number
}
